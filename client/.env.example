# Leave unset for local development (falls back to http://localhost:4000).
# Set this to your deployed backend URL when building for production, e.g.:
# VITE_API_URL=https://custos-api.onrender.com
VITE_API_URL=
